# Open Questions

Questions that need answers before work can proceed or assumptions that need validation.

---

- Will the mock API be replaced with a real backend? If so, what protocol (REST/GraphQL)?
- Are there plans to add write operations (create/update/delete for products and orders)?
- Is there a design system or Figma file driving the UI decisions?
